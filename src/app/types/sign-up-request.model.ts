export interface SignUpRequest {
  email: string;
  newPassword: string;
  firstName: string;
  lastName: string;
  phoneNumber: string;
  yearOfBirth: string;
  city: string;
  accessToken: string;
}
